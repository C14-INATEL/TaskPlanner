# URL base da API do backend (Flask).
# Em desenvolvimento local copie este arquivo para .env.local:
#   cp .env.example .env.local
# Em produção, defina esta variável no ambiente de deploy (ex: Vercel).
NEXT_PUBLIC_API_URL=http://localhost:5000
